apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job
  namespace: job
spec:
  template:
    spec:
      containers:
      - name: hello
        image: busybox
        command: ["echo", "Hello from Argo CD Job!"]
      restartPolicy: Never
  backoffLimit: 4