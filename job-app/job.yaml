apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job               # fixed name
  namespace: jobs
  labels:
    app.kubernetes.io/name: hello-job
spec:
  selector:
    matchLabels:
      controller-uid: hello-job-uid          # must match template
  template:
    metadata:
      labels:
        controller-uid: hello-job-uid
        app.kubernetes.io/name: hello-job
    spec:
      restartPolicy: Never
      containers:
      - name: hello
        image: busybox
        command:
          - sh
          - -c
          - |
            echo "Hello from Argo CD Job! $(date -u)"
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600