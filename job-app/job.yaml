apiVersion: batch/v1
kind: Job
metadata:
  generateName: hello-job-
  namespace: job1
  labels:
    app: hello-job
spec:
  template:
    metadata:
      labels:
        app: hello-job
    spec:
      containers:
      - name: hello
        image: busybox
        command: ["echo", "Hello from Argo CD Job!"]
      restartPolicy: Never
  backoffLimit: 0